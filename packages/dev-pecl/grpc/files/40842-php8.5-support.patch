Upstream: no, https://github.com/grpc/grpc/pull/40842

From 9211690d4661898fea13caeebeefc4f736db1376 Mon Sep 17 00:00:00 2001
From: Ajinkya Kulkarni <kajinkya@google.com>
Date: Mon, 6 Oct 2025 06:54:35 +0000
Subject: [PATCH] PHP-alpha version zend exception update

---
 src/php/ext/grpc/call.c | 8 +++++---
 1 file changed, 5 insertions(+), 3 deletions(-)

diff --git a/src/php/ext/grpc/call.c b/src/php/ext/grpc/call.c
index 7bc711be67d23..cfe47d1e23741 100644
--- a/src/php/ext/grpc/call.c
+++ b/src/php/ext/grpc/call.c
@@ -84,9 +84,11 @@ zval *grpc_parse_metadata_array(grpc_metadata_array
     if (php_grpc_zend_hash_find(array_hash, str_key, key_len, (void **)&data)
         == SUCCESS) {
       if (Z_TYPE_P(data) != IS_ARRAY) {
-        zend_throw_exception(zend_exception_get_default(TSRMLS_C),
-                             "Metadata hash somehow contains wrong types.",
-                             1 TSRMLS_CC);
+        #if PHP_VERSION_ID >= 80500
+            zend_throw_exception(zend_ce_exception, "Metadata hash somehow contains wrong types.", 1);
+        #else
+            zend_throw_exception(zend_exception_get_default(TSRMLS_C), "Metadata hash somehow contains wrong types.", 1 TSRMLS_CC);
+        #endif
         efree(str_key);
         efree(str_val);
         PHP_GRPC_FREE_STD_ZVAL(array);
